gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
**/*.keystore
**/*.jks
# Deliberately committed (private repo): the app key is the promoted debug
# keystore (well-known debug passwords, no Play Store) — losing it would break
# in-app updates on every installed device. key.properties is committed too.
!app/frt-upload.keystore
